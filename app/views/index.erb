<% @posts.each do |post| %>
  <div class="post <%= post.slug %>">
    <header>
      <h2>
        <a href="/<%= post.slug %>"><%= post.title %></a>
      </h2>

      <% if post.date %>
        <time datatime="<%= post.date.to_s %>">
          <a href="/<%= post.slug %>">
            <%= post.date.to_s(:short_ordinal) %>
          </a>
        </time>
      <% end %>

      <% if post.author %>
        <h3 class="author">
          by <%= post.author %>
        </h3>
      <% end %>
    </header>

    <div class="body">
      <%== post.render %>
    </div>
  </div>
<% end %>

<% content_for :side_bar do %>
  sidebar
<% end %>

<% content_for :footer do %>
  <div class="pagination">
    <div class="wrap">
      <% if paginate_next?(@posts) || paginate_previous? %>
        <% if paginate_previous? %>
          <a class="button" href="<%= paginate_previous_url %>">
            <i class="fa fa-long-arrow-left"></i>
            Previous
          </a>
        <% end %>

        <% if paginate_next?(@posts) %>
          <a class="button" href="<%= paginate_next_url %>">
            Next
            <i class="fa fa-long-arrow-right"></i>
          </a>
        <% end %>
      <% end %>
    </div>
  </div>
<% end %>
